/* global process, console */
import { createHash } from 'node:crypto'
import { Buffer } from 'node:buffer'
import { mkdir, readFile, writeFile } from 'node:fs/promises'
import path from 'node:path'

const PIN={version:'4.5.4.4.4',commit:'b3f38149a9e5ffbba1eae3a9f2ddcdd66481884c',parser:'14e3edc89ed705bd4e4eda5c8135756431c76e81',files:{'version.txt':'ec563ae02051174b61a31ae1a0aa84329cd1025cc5ce2ecde19fae85b446ba28','data/mods.json':'33e65dd81cc430c067b70550d4b7a6360b66ae477ced225cdc6309d58acafe1b','data/mods_by_base.json':'80ca517eca450192e88e86cb550af8cc43ccb6d0b29ecc4c2537ac67d47db4b9','data/base_items.json':'846029974c5cd643423404072bd6c5b497c5ed129544750a41ca7d0e0c5b4449','data/item_classes.json':'f1905c443d45863ad66bc07066b63551c7ed315f77e6b635fee2fa52ee05d821','data/tags.json':'37f16501f1838d928e8b62616fbed3a6c70b5dd6c8e657e6fe47f08b4ef5ab92'}}
const CATEGORIES={Jewels:{kind:'jewel',scope:'poe2-technical-jewel-mod-data-for-build-planner',classId:'Jewels'},Charms:{kind:'charm',scope:'poe2-technical-charm-mod-data-for-build-planner',classId:'Charms'},'Life Flasks':{kind:'life-flask',scope:'poe2-technical-flask-mod-data-for-build-planner',classId:'Life Flasks'},'Mana Flasks':{kind:'mana-flask',scope:'poe2-technical-flask-mod-data-for-build-planner',classId:'Mana Flasks'}}
const argv=process.argv.slice(2),args={};for(let i=0;i<argv.length;i++)if(argv[i].startsWith('--'))args[argv[i].slice(2)]=argv[++i]
if(!args['source-dir']||args.version!==PIN.version||args.commit!==PIN.commit||args['parser-commit']!==PIN.parser)throw new Error('Explicit approved source pin required')
const root=process.cwd(),out=path.join(root,'generated','poe2-items'),sha=v=>createHash('sha256').update(v).digest('hex'),stable=v=>JSON.stringify(v,null,2)+'\n',buffers={}
for(const[file,expected]of Object.entries(PIN.files)){const value=await readFile(path.join(args['source-dir'],...file.split('/')));if(sha(value)!==expected)throw new Error(`SHA-256 mismatch: ${file}`);buffers[file]=value}
if(buffers['version.txt'].toString().trim()!==PIN.version)throw new Error('Version mismatch')
const approval=JSON.parse(await readFile(path.join(root,'data-sources','source-approval.json'),'utf8')),source=approval.reviewedSources.find(x=>x.sourceId==='repoe-poe2')
if(source?.status!=='conditionally-approved')throw new Error('Approval source guard rejected import')
for(const[name,config]of Object.entries(CATEGORIES)){const rule=approval.categoryAssignments.find(x=>x.categoryId===config.scope);if(rule?.status!=='conditionally-approved'||rule.primarySourceId!=='repoe-poe2'||!rule.constraints?.allowedItemCategories?.includes(name))throw new Error(`Approval category guard rejected ${name}`);for(const file of Object.keys(PIN.files))if(!rule.constraints.allowedSourceFiles.includes(file))throw new Error(`Approval file guard rejected ${name}:${file}`)}
const parse=f=>JSON.parse(buffers[f]),mods=parse('data/mods.json'),byBase=parse('data/mods_by_base.json'),bases=parse('data/base_items.json'),classes=parse('data/item_classes.json')
const old=JSON.parse(await readFile(path.join(root,'generated','poe2-affixes','technical-affixes.json'),'utf8')),oldById=new Map(old.map(x=>[x.affixId,x])),refs=new Map(),categoryRefs={},warnings=[]
const add=(id,category,side,family,baseItemId=null)=>{const value=refs.get(id)??{categories:new Set(),sides:new Set(),families:new Set(),baseItemIds:new Set()};value.categories.add(category);value.sides.add(side);if(family)value.families.add(family);if(baseItemId)value.baseItemIds.add(baseItemId);refs.set(id,value)}
for(const category of Object.keys(CATEGORIES)){const categoryIds=new Set();for(const group of Object.values(byBase[category]??{}))for(const side of ['prefix','suffix'])for(const[family,members]of Object.entries(group.mods?.[side]??{}))for(const id of Object.keys(members??{})){add(id,category,side,family);categoryIds.add(id)}categoryRefs[category]=categoryIds}
const classNameById=new Map(Object.entries(classes).map(([id,v])=>[id,v.name])),baseItems=[]
for(const[id,base]of Object.entries(bases).sort(([a],[b])=>a.localeCompare(b))){const category=classNameById.get(base.item_class);if(!CATEGORIES[category]||base.release_state!=='released')continue;const properties=Object.fromEntries(Object.entries(base.properties??{}).filter(([key,value])=>value!==null&&['charges_max','charges_per_use','duration','life_per_use','mana_per_use'].includes(key)));baseItems.push({baseItemId:id,itemClassId:category,itemKind:CATEGORIES[category].kind,dropLevel:base.drop_level??null,properties,tags:[...(base.tags??[])].sort(),implicitModIds:category==='Charms'?[...(base.implicits??[])].sort():[],sourceVersion:PIN.version,sourceReference:`data/base_items.json#${id}`,dataStatus:'available',localizationStatus:'translation-missing'});if(category==='Charms')for(const implicit of base.implicits??[])add(implicit,category,'implicit',mods[implicit]?.type,id)}
const sideOf=s=>s.includes('prefix')?'prefix':s.includes('suffix')?'suffix':s.includes('implicit')?'implicit':'unknown',technicalMods=[]
for(const[id,ref]of [...refs].sort(([a],[b])=>a.localeCompare(b))){const mod=mods[id];if(!mod){warnings.push(`orphan-mod-reference:${id}`);continue}if(oldById.has(id))throw new Error(`Duplicate existing 5M.1 mod ID: ${id}`);const sides=[...ref.sides].sort(),families=[...ref.families].sort(),statLines=(mod.stats??[]).map((stat,index)=>({statId:stat.id,minimum:stat.min,maximum:stat.max,numericType:Number.isInteger(stat.min)&&Number.isInteger(stat.max)?'integer':'decimal',valueType:stat.min===stat.max?'fixed':'range',isLocal:String(stat.id).toLowerCase().includes('local'),statOrder:index,sourceReference:`data/mods.json#${id}.stats[${index}]`,localizationStatus:'translation-missing'}));technicalMods.push({affixId:id,sourceModId:id,familyId:families[0]??mod.type??id,tierId:null,generationType:mod.generation_type??'unknown',affixSide:sideOf(sides),domain:mod.domain??'unknown',itemClassIds:[...ref.categories].sort(),baseItemIds:[...ref.baseItemIds].sort(),requiredItemLevel:mod.required_level??null,tags:[...(mod.adds_tags??[]),...(mod.implicit_tags??[])].sort(),spawnWeights:mod.spawn_weights??[],statLines,groups:[...(mod.groups??[])].sort(),conflictGroups:[...(mod.groups??[])].sort(),isHybrid:statLines.length>1,isLocal:statLines.length?statLines.every(x=>x.isLocal):null,sourceVersion:PIN.version,sourceReference:`data/mods.json#${id}`,dataStatus:statLines.length?'available':'partially-supported',localizationStatus:'translation-missing',technicalName:id,technicalText:id,sourceSides:sides})}
const grouped=new Map();for(const mod of technicalMods){const key=`${mod.affixSide}:${mod.familyId}`,list=grouped.get(key)??[];list.push(mod);grouped.set(key,list)}
for(const[key,list]of grouped){list.sort((a,b)=>(a.requiredItemLevel??-1)-(b.requiredItemLevel??-1)||a.affixId.localeCompare(b.affixId));list.forEach((mod,index)=>mod.tierId=`${key}:tier-${index+1}`)}
const modsByKind=kind=>technicalMods.filter(x=>x.itemClassIds.some(c=>CATEGORIES[c]?.kind===kind)),basesByKind=kind=>baseItems.filter(x=>x.itemKind===kind)
const index=Object.fromEntries(Object.keys(CATEGORIES).sort().map(category=>[category,{baseItemIds:baseItems.filter(x=>x.itemClassId===category).map(x=>x.baseItemId),prefix:technicalMods.filter(x=>x.itemClassIds.includes(category)&&x.affixSide==='prefix').map(x=>x.affixId),suffix:technicalMods.filter(x=>x.itemClassIds.includes(category)&&x.affixSide==='suffix').map(x=>x.affixId),implicit:technicalMods.filter(x=>x.itemClassIds.includes(category)&&x.affixSide==='implicit').map(x=>x.affixId)}]))
const coverage={countingRules:{reference:'unique mod ID per category across prefix/suffix maps',uniqueMod:'unique generated affixId across categories plus approved Charm base implicits',tier:'one generated record per technical mod',statLine:'one source stats entry'},categories:{}}
for(const category of Object.keys(CATEGORIES)){const selected=technicalMods.filter(x=>x.itemClassIds.includes(category));coverage.categories[category]={references:categoryRefs[category].size,uniqueMods:selected.length,newMods:selected.length,existingMods:0,baseItems:baseItems.filter(x=>x.itemClassId===category).length,prefixes:selected.filter(x=>x.affixSide==='prefix').length,suffixes:selected.filter(x=>x.affixSide==='suffix').length,implicits:selected.filter(x=>x.affixSide==='implicit').length,tiers:selected.length,statLines:selected.reduce((n,x)=>n+x.statLines.length,0),multiLine:selected.filter(x=>x.statLines.length>1).length,hybridMods:selected.filter(x=>x.isHybrid).length,conflictGroups:new Set(selected.flatMap(x=>x.conflictGroups)).size,uiSelectableMods:selected.length,engineProcessableMods:0,unsupportedMods:selected.length,translationMissing:selected.length,orphanReferences:0,excluded:['uniques','socketables','relics','corrupted','instilled','desecrated','mutated']}}
const outputs={'jewel-base-items.json':basesByKind('jewel'),'jewel-mods.json':modsByKind('jewel'),'charm-base-items.json':basesByKind('charm'),'charm-mods.json':modsByKind('charm'),'life-flask-base-items.json':basesByKind('life-flask'),'life-flask-mods.json':modsByKind('life-flask'),'mana-flask-base-items.json':basesByKind('mana-flask'),'mana-flask-mods.json':modsByKind('mana-flask'),'additional-item-class-index.json':index,'additional-item-coverage-report.json':coverage,'additional-item-import-report.json':{schemaVersion:1,warnings:warnings.sort(),overlapWithExisting5M1:0,excludedCategories:['relics','uniques','socketables','corrupted','instilled','desecrated','mutated','skills','supports','german-texts','raw-export-mirror']}}
await mkdir(out,{recursive:true});const generated=[];for(const[name,value]of Object.entries(outputs)){const text=stable(value);await writeFile(path.join(out,name),text);generated.push({relativePath:name,sha256:sha(text),sizeBytes:Buffer.byteLength(text)})}
const manifest={schemaVersion:1,importFormatVersion:1,approvalScopes:[...new Set(Object.values(CATEGORIES).map(x=>x.scope))],approvalStatus:'conditionally-approved',exportRepository:'repoe-fork/poe2',exportVersion:PIN.version,exportCommit:PIN.commit,parserRepository:'repoe-fork/repoe',parserCommit:PIN.parser,sourceFiles:Object.entries(PIN.files).map(([relativePath,hash])=>({relativePath,sha256:hash,sizeBytes:buffers[relativePath].length,usedFields:relativePath.endsWith('mods.json')?['generation_type','domain','required_level','adds_tags','implicit_tags','spawn_weights','stats','groups','type']:relativePath.endsWith('mods_by_base.json')?['bases','mods','prefix','suffix']:relativePath.endsWith('base_items.json')?['item_class','drop_level','implicits','properties','release_state','tags']:relativePath.endsWith('item_classes.json')?['name']:[]})),generatedFiles:generated,excludedCategories:['raw-export-mirror','uniques','relics','socketables','skills','supports','display-names','german-texts','media'],attribution:'Technical data derived from RePoE; game data and content are property of Grinding Gear Games.',rightsNotice:'Conditionally approved project scope; no general data license or legal guarantee is asserted.',removable:true}
await writeFile(path.join(out,'additional-item-source-manifest.json'),stable(manifest));console.log(JSON.stringify({coverage:coverage.categories,generatedFiles:generated},null,2))
