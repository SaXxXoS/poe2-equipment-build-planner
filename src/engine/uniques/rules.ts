import type { MechanicTag, UniqueItemSlot } from '../../domain';import type{ScoreCategory}from'../common/types'
export interface UniqueRule{ruleId:string;descriptionKey:string;applicableItemSlots?:UniqueItemSlot[];requiredUniqueTags?:MechanicTag[];excludedUniqueTags?:MechanicTag[];requiredProfileFields?:string[];affectedScoreCategory:ScoreCategory;weight:number;threshold?:number;maximumContribution?:number;replacementWeight?:number;tradeOffWeight?:number;reasonCode:string;enabled:boolean}
const r=(tag:MechanicTag,field:string,category:ScoreCategory='damage'):UniqueRule=>({ruleId:`unique-${tag}`,descriptionKey:`engine.unique.rule.${tag}`,requiredUniqueTags:[tag],requiredProfileFields:[field],affectedScoreCategory:category,weight:.4,threshold:1,maximumContribution:40,replacementWeight:1,tradeOffWeight:1,reasonCode:`unique-${tag}-synergy`,enabled:true})
export const UNIQUE_RULES:UniqueRule[]=[r('physical','damageTypes.physical'),r('fire','damageTypes.fire'),r('cold','damageTypes.cold'),r('lightning','damageTypes.lightning'),r('chaos','damageTypes.chaos'),r('attack','mechanics.attack'),r('spell','mechanics.spell'),r('projectile','mechanics.projectile'),r('melee','mechanics.melee'),r('area','mechanics.area'),r('critical','mechanics.critical'),r('damage-over-time','mechanics.damage-over-time'),r('minion','mechanics.minion'),r('defensive','defence.generalDefenceNeed','defence'),r('resistance','defence.resistanceNeed','defence'),r('resource','requirements.resourceNeed','resource'),r('strength','requirements.strengthNeed','resource'),r('dexterity','requirements.dexterityNeed','resource'),r('intelligence','requirements.intelligenceNeed','resource')]
