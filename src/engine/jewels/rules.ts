import type { JewelType, MechanicTag } from '../../domain'
import type { ScoreCategory } from '../common/types'
export interface JewelRule { ruleId: string; descriptionKey: string; applicableJewelTypes: JewelType[]; requiredJewelTags?: MechanicTag[]; excludedJewelTags?: MechanicTag[]; requiredProfileFields?: string[]; affectedScoreCategory: ScoreCategory; weight: number; threshold?: number; maximumContribution?: number; pathCostWeight?: number; reasonCode: string; enabled: boolean }
const rule = (tag: MechanicTag, field: string, category: ScoreCategory = 'damage'): JewelRule => ({ ruleId: `jewel-${tag}`, descriptionKey: `engine.jewel.rule.${tag}`, applicableJewelTypes: ['normal', 'cluster', 'unique-cluster'], requiredJewelTags: [tag], requiredProfileFields: [field], affectedScoreCategory: category, weight: 0.4, threshold: 1, maximumContribution: 40, reasonCode: `jewel-${tag}-synergy`, enabled: true })
export const JEWEL_RULES: JewelRule[] = [rule('physical','damageTypes.physical'),rule('fire','damageTypes.fire'),rule('cold','damageTypes.cold'),rule('lightning','damageTypes.lightning'),rule('chaos','damageTypes.chaos'),rule('attack','mechanics.attack'),rule('spell','mechanics.spell'),rule('projectile','mechanics.projectile'),rule('melee','mechanics.melee'),rule('area','mechanics.area'),rule('critical','mechanics.critical'),rule('damage-over-time','mechanics.damage-over-time'),rule('minion','mechanics.minion'),rule('defensive','defence.generalDefenceNeed','defence'),rule('resistance','defence.resistanceNeed','defence'),rule('resource','requirements.resourceNeed','resource'),rule('strength','requirements.strengthNeed','resource'),rule('dexterity','requirements.dexterityNeed','resource'),rule('intelligence','requirements.intelligenceNeed','resource')]
