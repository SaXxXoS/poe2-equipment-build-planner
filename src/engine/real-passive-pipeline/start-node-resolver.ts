import type { RealPassivePipelineInput,StartNodeResolution } from './types'
const classes=(value:number|number[]|null)=>value===null?[]:(Array.isArray(value)?value:[value]).map(String)
export function resolveRealPassiveStartNode(input:RealPassivePipelineInput):StartNodeResolution{const starts=input.passiveTree.nodes.filter(node=>node.isClassStart);if(input.startNodeId){const node=starts.find(value=>value.id===input.startNodeId);const violations=!node?['explicit-start-not-found']:!classes(node.classStartIndex).includes(input.characterClassId)?['explicit-start-class-mismatch']:[];return{resolvedStartNodeId:violations.length?null:input.startNodeId,resolutionSource:violations.length?null:'explicit',classId:input.characterClassId,warnings:[],violations}}const matches=starts.filter(node=>classes(node.classStartIndex).includes(input.characterClassId));return{resolvedStartNodeId:matches.length===1?matches[0].id:null,resolutionSource:matches.length===1?'official-class-mapping':null,classId:input.characterClassId,warnings:[],violations:matches.length===0?['class-start-not-found']:matches.length>1?['ambiguous-class-start']:[]}}
