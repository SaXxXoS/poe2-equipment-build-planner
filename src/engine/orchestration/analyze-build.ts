import type { ModifierDefinition } from '../../domain'
import { equipmentAnalyzer } from '../equipment/analyzer'
import { skillAnalyzer } from '../skills/analyzer'
import { supportAnalyzer } from '../supports/analyzer'
import { passiveAnalyzer } from '../passives/analyzer'
import { jewelAnalyzer } from '../jewels/analyzer'
import { uniqueAnalyzer } from '../uniques/analyzer'
import { rotationGenerator } from '../rotations/generator'
import { explanationGenerator } from '../explanations/generator'
import type { AnalyzerContext, BuildAnalysis, EngineRequest } from '../common/types'
import { runRealPassivePlanningIntegration } from './real-passive-integration'
export const ENGINE_VERSION = '0.1.0-placeholder'
export function analyzeBuild(request: EngineRequest, context: AnalyzerContext = { engineVersion: ENGINE_VERSION, fixtureMode: true }, modifiers: ModifierDefinition[] = []): BuildAnalysis {
  const orchestratorStarted=performance.now(),moduleTrace: string[] = []; const runtime = { ...context, trace: moduleTrace }; const equipment = equipmentAnalyzer.analyze(request, runtime, modifiers)
  const realPassivePlanning=runRealPassivePlanningIntegration(request.realPassivePlanning,equipment.value.buildProfile,runtime)
  const skillAnalysis = skillAnalyzer.analyzeRanked(equipment.value.buildProfile, request.candidates.skills, runtime, { equipmentAnalysis: equipment.value.equipmentAnalysis, character: request.input.character, goalProfile: request.input.goalProfile, availableWeaponTypes: ['any'] }); const skills = skillAnalysis.allCandidates; const selectedRecommendation = skills.find(item => item.valid); const selectedSkill = request.candidates.skills.find(item => item.id === selectedRecommendation?.skillId) ?? request.candidates.skills[0]
  const supportAnalysis = selectedSkill ? supportAnalyzer.analyzeRanked(selectedSkill, equipment.value.buildProfile, request.candidates.supports, runtime, { skillRecommendation: selectedRecommendation, equipmentAnalysis: equipment.value.equipmentAnalysis, character: request.input.character, goalProfile: request.input.goalProfile, availableWeaponTypes: ['any'] }) : { allCandidates: [], eligibleCandidates: [], blockedCandidates: [], topCandidates: [], topDamageSupports: [], topMappingSupports: [], topBossSupports: [], topUtilitySupports: [], topDefensiveSupports: [], status: 'placeholder' as const, analyzerVersion: 'none' }; const supports = supportAnalysis.allCandidates
  const selectedSupports = supportAnalysis.topCandidates; const passiveAnalysis = passiveAnalyzer.analyzeRanked(equipment.value.buildProfile, request.candidates.passives, runtime, { equipmentAnalysis: equipment.value.equipmentAnalysis, skillRecommendation: selectedRecommendation, supportRecommendations: selectedSupports, character: request.input.character, goalProfile: request.input.goalProfile }); const passives = passiveAnalysis.allCandidates
  const jewelAnalysis = jewelAnalyzer.analyzeRanked(equipment.value.buildProfile, request.candidates.jewels, runtime, { equipmentAnalysis: equipment.value.equipmentAnalysis, skillRecommendation: selectedRecommendation, supportRecommendations: selectedSupports, passiveAnalysis, character: request.input.character, technicalSelections:request.input.selectedJewels }); const jewels = jewelAnalysis.allCandidates; const uniqueAnalysis = uniqueAnalyzer.analyzeRanked(equipment.value.buildProfile, request.input, request.candidates.uniques, runtime, { equipmentAnalysis: equipment.value.equipmentAnalysis, skillRecommendation: selectedRecommendation, supportRecommendations: selectedSupports, passiveAnalysis, jewelAnalysis, availableWeaponTypes: ['any'] }); const uniques = uniqueAnalysis.allCandidates
  const selectedRotationSkills = skillAnalysis.eligibleCandidates.filter((item, index, all) => { const definition = request.candidates.skills.find(skill => skill.id === item.skillId); const role = definition?.tags.includes('debuff') ? 'debuff' : definition?.tags.includes('buff') ? 'buff' : definition?.tags.includes('movement') ? 'movement' : definition?.tags.includes('defensive') ? 'defensive' : item.recommendedRole; return all.findIndex(candidate => { const candidateDefinition = request.candidates.skills.find(skill => skill.id === candidate.skillId); const candidateRole = candidateDefinition?.tags.includes('debuff') ? 'debuff' : candidateDefinition?.tags.includes('buff') ? 'buff' : candidateDefinition?.tags.includes('movement') ? 'movement' : candidateDefinition?.tags.includes('defensive') ? 'defensive' : candidate.recommendedRole; return candidateRole === role }) === index })
  const rotationAnalysis = rotationGenerator.generate({ buildProfile: equipment.value.buildProfile, skillAnalysis, selectedSkills: selectedRotationSkills, skillDefinitions: request.candidates.skills, selectedSupports, uniqueAnalysis, profileClarity: equipment.value.equipmentAnalysis.profileClarity, availableWeaponSets: ['set-1', 'set-2'] }, runtime); const rotations = { rotationAnalysis, mappingRotation: rotationAnalysis.mappingRotation, bossRotation: rotationAnalysis.bossRotation }; const allRecommendations = [...skills, ...supports, ...passives, ...jewels, ...uniques]
  const warnings = [...equipment.violations, ...skillAnalysis.allCandidates.flatMap(item => item.warnings), ...supportAnalysis.allCandidates.flatMap(item => item.warnings), ...passiveAnalysis.allCandidates.flatMap(item => item.warnings), ...allRecommendations.flatMap(item => item.violations), ...rotationAnalysis.warnings, ...rotationAnalysis.violations]
  const displayNames = Object.fromEntries([...request.candidates.skills, ...request.candidates.supports, ...request.candidates.jewels, ...request.candidates.uniques, ...request.candidates.passives.flatMap(item => [...(item.nodes ?? []), ...(item.cluster ? [{ id: item.cluster.clusterId, displayNameDe: item.cluster.clusterId }] : [])])].map(item => [item.id, item.displayNameDe])); const explanations = explanationGenerator.generate({ reasons: [...equipment.reasons, ...allRecommendations.flatMap(item => item.reasons)], violations: warnings, equipmentAnalysis: equipment.value.equipmentAnalysis, skillAnalysis, supportAnalysis, passiveAnalysis, jewelAnalysis, uniqueAnalysis, rotationAnalysis, displayNames }, runtime)
  if(realPassivePlanning?.performance)realPassivePlanning.performance.orchestratorDurationMs=performance.now()-orchestratorStarted
  return { equipmentAnalysis: equipment.value.equipmentAnalysis, buildProfile: equipment.value.buildProfile, skillAnalysis, skillRecommendations: skills, supportAnalysis, supportRecommendations: supports, passiveAnalysis, passiveRecommendations: passives, jewelAnalysis, jewelRecommendations: jewels, uniqueAnalysis, uniqueRecommendations: uniques, ...rotations, explanations, warnings, status: 'placeholder', engineVersion: context.engineVersion, moduleTrace, ...(realPassivePlanning?{realPassivePlanning}:{}) }
}
