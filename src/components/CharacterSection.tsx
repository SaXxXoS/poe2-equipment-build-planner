import type { Character } from '../models'; import { ascendancies, classes } from '../data'
export function CharacterSection({value,onChange}:{value:Character;onChange:(v:Character)=>void}){const available=ascendancies.filter(a=>a.classId===value.classId); return <section><h2>1. Charakter</h2><div className="form-grid"><label>Klasse<select value={value.classId} onChange={e=>{const classId=e.target.value;onChange({...value,classId,ascendancyId:ascendancies.find(a=>a.classId===classId)?.id??''})}}>{classes.map(c=><option key={c}>{c}</option>)}</select></label><label>Aszendenz<select value={value.ascendancyId} onChange={e=>onChange({...value,ascendancyId:e.target.value})}>{available.map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><label>Charakterlevel<input aria-label="Charakterlevel" type="number" min="1" max="100" value={value.level} onChange={e=>onChange({...value,level:Number(e.target.value)})}/></label><label>Ziel<select value={value.goal} onChange={e=>onChange({...value,goal:e.target.value as Character['goal']})}><option>Ausgeglichen</option><option>Mapping</option><option>Boss</option></select></label><label>Hauptfertigkeit<select value={value.mainSkill} onChange={e=>onChange({...value,mainSkill:e.target.value})}><option>Automatisch</option><option>Blitzpfeil</option><option>Kugelblitz</option><option>Sprungschlag</option></select></label></div></section>}
