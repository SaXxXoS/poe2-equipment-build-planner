import type { Skill } from '../models'
export function SkillsSection({skills,onChange}:{skills:Skill[];onChange:(v:Skill[])=>void}){return <section><h2>3. Fertigkeitsgems & Unterstützungen</h2><div className="skills-grid">{skills.map((s,i)=><article className="skill-card" key={s.id}><div className="row"><h3>{s.name}</h3><span className={`badge ${s.role==='Hauptfertigkeit'?'main':''}`}>{s.role}</span></div><label>Waffenbelegung<select value={s.weaponSet} onChange={e=>onChange(skills.map((x,j)=>j===i?{...x,weaponSet:e.target.value as Skill['weaponSet']}:x))}><option>Waffen-Set 1</option><option>Waffen-Set 2</option><option>Beide</option></select></label><div className="supports">{s.supports.map((g,j)=><span key={j} title={g?.name}>{j+1}<small>{g?.name}</small></span>)}</div></article>)}</div></section>}
