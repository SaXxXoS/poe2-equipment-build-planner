import { useMemo,useState } from 'react'; import { affixes } from '../data'; import type { Affix,EquipmentSlot } from '../models'
export function AffixDialog({slot,onSave,onClose}:{slot:EquipmentSlot;onSave:(a:Affix[])=>void;onClose:()=>void}){const [search,setSearch]=useState('');const [selected,setSelected]=useState(affixes[0].id);const [value,setValue]=useState(10);const [added,setAdded]=useState(slot.affixes);const visible=useMemo(()=>affixes.filter(a=>a.name.toLowerCase().includes(search.toLowerCase())),[search]);function add(){const found=affixes.find(a=>a.id===selected);if(found)setAdded([...added,{...found,id:`${found.id}-${Date.now()}`,value}])}return <div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="modal" role="dialog" aria-modal="true"><div className="row"><h2>{slot.name}</h2><button className="icon" aria-label="Dialog schließen" onClick={onClose}>×</button></div><label>Affixe suchen<input autoFocus value={search} onChange={e=>setSearch(e.target.value)} placeholder="z. B. Widerstand"/></label><label>Affix<select className="affix-list" size={Math.min(6,Math.max(2,visible.length))} value={selected} onChange={e=>setSelected(e.target.value)}>{visible.map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><label>Wert<input aria-label="Affixwert" type="number" value={value} onChange={e=>setValue(Number(e.target.value))}/></label><button onClick={add}>Affix hinzufügen</button><h3>Gespeicherte Affixe</h3>{added.length===0?<p className="muted">Noch keine Affixe.</p>:<ul className="clean">{added.map((a,i)=><li key={`${a.id}-${i}`}><span>{a.name}: <b>{a.value}</b></span><button className="danger" aria-label={`${a.name} entfernen`} onClick={()=>setAdded(added.filter((_,x)=>x!==i))}>Entfernen</button></li>)}</ul>}<div className="dialog-actions"><button className="secondary" onClick={onClose}>Abbrechen</button><button onClick={()=>{onSave(added);onClose()}}>Speichern</button></div></div></div>}
