import {useState} from 'react'; import './styles.css'; import type {Character,EquipmentSlot} from './models'; import {ascendancies,buildResult,equipmentNames,skills as initialSkills} from './data'; import {CharacterSection} from './components/CharacterSection';import {EquipmentSection} from './components/EquipmentSection';import {SkillsSection} from './components/SkillsSection';import {JewelsSection} from './components/JewelsSection';import {PassiveTree} from './components/PassiveTree';import {BuildResultSection} from './components/BuildResultSection'
export default function App(){const [character,setCharacter]=useState<Character>({classId:'Krieger',ascendancyId:ascendancies[0].id,level:70,goal:'Ausgeglichen',mainSkill:'Automatisch'});const [slots,setSlots]=useState<EquipmentSlot[]>(equipmentNames.map((name,i)=>({id:`slot-${i}`,name,affixes:[]})));const [skillState,setSkills]=useState(initialSkills);const [calculated,setCalculated]=useState(false);function calculate(){setCalculated(true);setTimeout(()=>document.querySelector('#result')?.scrollIntoView({behavior:'smooth'}),0)}return <><header><p className="eyebrow">Equipment-first · Mobiler Prototyp</p><h1>PoE2 Equipment Build Planner</h1><p>Plane einen Build ausgehend von deiner Ausrüstung. Alle Daten und Ergebnisse dieser Version sind lokale Platzhalter.</p></header><main><CharacterSection value={character} onChange={setCharacter}/><EquipmentSection slots={slots} setSlots={setSlots}/><SkillsSection skills={skillState} onChange={setSkills}/><JewelsSection/><PassiveTree/><section className="calculate"><h2>6. Test-Build berechnen</h2><p>Die aktuelle Version verwendet keine echte Optimierungs-Engine.</p><button className="calculate-btn" onClick={calculate}>Build berechnen</button></section>{calculated&&<BuildResultSection result={buildResult}/>}</main><footer>Prototyp mit lokalen Platzhalterdaten · Keine Verbindung zu PoE2DB</footer></>}
